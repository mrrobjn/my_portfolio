type LayoutProp = {
    children: ReactNode;
};

type NavItemProp = {
    name: string,
    link: string,
}

type IconLinkProp = {
    icon: string,
    link: string,
}

type SectionTitleProp = {
    mainTitle: string,
    subTitle: string,
}

type CareerItemProp = {
    company: string,
    duration: string,
    description: string,
    index: number,
}